# 录音SoC设计工程规划文档

## 1. 项目概述

### 1.1 项目目标
- 基于现有 `soc-wuxi-arm/` Cortex-M3 SoC 框架，新增录音数据采集与存储能力。
- 通过 I2S 接口接收 INMP441 数字麦克风输出的单声道音频数据，采样率 16kHz，仅使用左声道。
- 将录音数据写入片外存储器，满足 10s-30s 录音保存要求。
- 将存储数据导出到电脑，生成 `.wav` 文件并使用 PotPlayer 正常播放。

### 1.2 设计指标
- 音频输入器件：INMP441 数字麦克风
- 接口协议：I2S
- 声道：单声道，仅取左声道
- 采样率：16kHz
- 样本位宽：24bit PCM
- 录音时长：10s-30s
- 原始数据量估算：
  - 16kHz × 24bit × 1ch = 384 kbps ≈ 48 KB/s
  - 10s ≈ 480 KB
  - 30s ≈ 1.44 MB

### 1.3 基本要求 vs 进阶要求

#### 基本要求
- 通过 I2S 接口采样数据
- 将数据存储到 SDRAM 或 SPI FLASH
- 将录音结果导出到电脑并可播放

#### 进阶要求
- 由 CPU 将 PCM 数据打包成 WAV 格式
- 硬件实现 IMA-ADPCM 编码

### 1.4 预期最终成果
- 一套可在 Modelsim 仿真通过的录音 SoC RTL 修改版本
- 一套可在 Quartus 上板运行的 bitstream
- 一套 Keil CPU 工程代码，用于初始化外设、控制录音、触发导出
- 一组 testbench 与仿真波形结果
- 一份录音验证结果，包括原始数据导出文件、`.wav` 文件和播放截图或记录

### 1.5 主线实施策略
- 主线先完成“基本要求”，进阶功能作为尾期插入项，不影响主线验收。
- 主线先打通“简化数据通路”，再接入真实 I2S 音频。
- 存储方案建议优先选择 `SPI FLASH` 作为必须项实现路径。
  - 原因：30s 单声道 24bit/16kHz 原始 PCM 约 960KB，顺序写带宽需求仅 32KB/s，SPI FLASH 从实现复杂度上通常低于 SDRAM 控制器从零搭建。
  - 若开发板已有成熟 SDRAM 控制器、引脚约束和例程，可将主线切换为 SDRAM；本文档阶段划分对两种方案都适用。

## 2. 系统总体架构

### 2.1 SoC整体框图

```text
                +----------------------+
                |      Cortex-M3       |
                +----------+-----------+
                           |
                    AHB / APB Bus
                           |
        +------------------+------------------+
        |                                     |
   +----v-----+                         +-----v------+
   | On-chip  |                         | APB Subsys |
   | SRAM/ROM |                         +-----+------+
   +----------+                               |
                          +-------------------+-------------------+
                          |                   |                   |
                    +-----v-----+       +-----v-----+       +-----v------+
                    | I2S RX APB|       | Audio FIFO|       | Storage Ctl|
                    | registers |       | / status  |       |SPI/SDRAM   |
                    +-----+-----+       +-----+-----+       +------+-----+
                          |                   |                    |
                          +---------+---------+                    |
                                    |                              |
                               I2S sample data                     |
                                    |                              |
                              +-----v------+                 +-----v------+
                              | INMP441    |                 | SPI FLASH /|
                              | microphone |                 | SDRAM      |
                              +------------+                 +------------+
```

### 2.2 主要模块划分
- `top.v`
  - 增加 I2S 相关外部引脚
  - 集成新增 APB 外设或存储控制模块
- `apb_subsystem.v`
  - 利用现有 APB 扩展端口挂接新增录音相关外设
- `i2s_rx_core`
  - 完成 BCLK/LRCLK/SD 数据采样
  - 只提取左声道 24bit 数据
- `audio_fifo`
  - 缓冲 I2S 样本与 CPU/存储控制之间的速率差
- `audio_ctrl_apb`
  - 提供寄存器接口，供 CPU 配置、启动、状态读取和中断处理
- `storage_ctrl`
  - 主线推荐为 SPI FLASH 顺序写控制器
  - 备选为 SDRAM 写入控制器
- `cnasic_sleep`
  - 编写录音控制、存储管理、导出辅助代码

### 2.3 数据流向图

```text
INMP441
  -> I2S RX串并转换
  -> 左声道数据提取
  -> 音频FIFO
  -> 存储写入通路
  -> SPI FLASH / SDRAM
  -> 导出到PC
  -> 生成WAV
  -> PotPlayer播放
```

### 2.4 推荐的寄存器/地址规划
- 现有工程 APB 空间位于 `0x4000_0000` 起始区域。
- 现有 GPIO 已映射在 `0x4000_4000`，说明 APB 从设备按高位 nibble 进行 4KB 页粒度解码。
- 建议使用 APB 扩展端口挂接录音外设，优先使用：
  - `AudioCtrl`: `0x4000_C000` 对应扩展端口 12
  - `StorageCtrl`: `0x4000_D000` 对应扩展端口 13
- `AudioCtrl` 最小寄存器建议：
  - `CTRL`：使能、软复位、开始/停止
  - `STATUS`：FIFO 空/满、溢出、忙状态
  - `DATA`：CPU 读样本口，仅用于调试与最小闭环
  - `FIFO_LEVEL`：FIFO 深度监控
  - `INT_EN/INT_ST`：中断控制
- `StorageCtrl` 最小寄存器建议：
  - `START_ADDR`
  - `BYTE_LEN`
  - `CTRL`
  - `STATUS`
  - `DEBUG_CNT`

## 3. 开发流程与规范

### 3.1 开发原则
- 先仿真，后上板；任何新增模块必须先完成 testbench。
- 渐进式开发；先打通数据通路，再补功能细节。
- 先用简单可控数据验证链路，再接入真实麦克风数据。
- 先完成基本要求，再评估时间插入进阶要求。
- 每一阶段必须形成可见产出，禁止“只研究、不落地”的空转工作。

### 3.2 工具链
- RTL 开发与仿真：Modelsim
- FPGA 综合与上板：Quartus
- CPU 软件开发：Keil
- 数据导出与离线处理：建议使用 Python 或十六进制工具在 PC 端辅助处理

### 3.3 仿真与验证策略

#### 模块级验证
- I2S 接收模块：独立 testbench，输入理想 BCLK/LRCLK/SD 序列
- FIFO/寄存器模块：独立 testbench，覆盖满/空/阈值/溢出
- 存储控制模块：独立 testbench，连接行为模型或简化 memory model

#### 子系统级验证
- APB 访问录音外设寄存器
- CPU 或 AHB 主设备触发写存储
- 模拟连续音频样本流，检查地址递增与写入内容正确

#### SoC级验证
- 在 `soc-wuxi-arm/arm-soc/test.v` 基础上扩展顶层 testbench
- 先用固定计数样本代替真实 I2S 音频
- 再换成 I2S 帧级激励
- 最终检查导出存储内容与输入样本一致

#### 上板验证
- 先验证时钟、复位、寄存器读写、状态指示
- 再验证假数据录制
- 最后验证真实麦克风录音

### 3.4 代码与交付规范
- 每个新增 RTL 模块必须同时提交：
  - 源码
  - 对应 testbench
  - 关键波形截图
  - 使用说明
- 每次上板前，必须先在仿真中完成同版本验证。
- 每个阶段结束时更新：
  - 模块接口说明
  - 地址映射说明
  - 问题单与剩余风险

## 4. 开发阶段规划

### 阶段1：基础环境搭建与方案冻结

#### Phase目标
- 明确主线技术方案，补齐开发基线，建立后续开发的统一接口和地址规划。

#### 具体任务清单
- 梳理现有 `top.v`、`apb_subsystem.v`、`test.v`、`cnasic_sleep/App/main.c` 的可修改点
- 冻结主线存储方案：优先 SPI FLASH，备选 SDRAM
- 确认录音容量、地址映射、外部引脚和时钟来源
- 输出录音外设寄存器表、状态机草图、模块接口文档
- 在仿真工程中加入新增文件列表和编译脚本说明

#### 涉及模块
- `soc-wuxi-arm/arm-soc/top.v`
- `soc-wuxi-arm/arm-soc/apb/apb_subsystem.v`
- `soc-wuxi-arm/arm-soc/test.v`
- `soc-wuxi-arm/cnasic_sleep`

#### 验证策略（Testbench + 上板）
- Testbench：
  - 不做功能验证，重点验证工程能重新编译和加载程序
- 上板：
  - 不上板，仅确认 Quartus 工程可继续使用

#### 预期交付物
- 录音 SoC 详细方案说明
- 地址映射表
- 寄存器定义表
- 引脚清单与命名规则
- 更新后的阶段任务看板

#### 计划完成时间
- 第1天 - 第2天

#### 依赖关系
- 无

#### 验收标准
- 主线存储方案已冻结
- 新增模块边界和地址空间已明确
- 后续开发不再因“接口未定”反复返工

### 阶段2：I2S接收核心开发（必须）

#### Phase目标
- 完成 I2S 接收 RTL 核心，能从理想 I2S 时序中稳定提取左声道 24bit PCM 样本。

#### 具体任务清单
- 设计 `i2s_rx_core`
- 确定该模块与I2S接口以及AHB Matrix的连接端口
- 明确对 INMP441 的采样边沿、LRCLK 对齐关系、左声道提取规则
- 增加样本有效脉冲 `sample_valid`
- 支持固定 24bit 输出，屏蔽右声道数据
- 编写模块级 testbench，覆盖：
  - 正常左声道样本接收
  - 连续帧接收
  - 复位后重新同步
  - 左右声道切换边界

#### 涉及模块
- `i2s_rx_core.v`
- `tb_i2s_rx_core.v`

#### 验证策略（Testbench + 上板）
- Testbench：
  - 使用已知样本序列生成 I2S 时钟和串行数据
  - 对比输出 PCM 与期望值
- 上板：
  - 暂不上板

#### 预期交付物
- I2S 接收核心 RTL
- 独立 testbench
- 仿真波形截图
- I2S 时序说明文档

#### 计划完成时间
- 第3天 - 第4天

#### 依赖关系
- 依赖阶段1的接口和时钟方案冻结

#### 验收标准
- 模块在仿真中能连续输出正确左声道 24bit 样本
- 不出现 bit slip、帧错位、复位后不同步问题

### 阶段3：APB寄存器 + FIFO + 中断骨架开发（必须）

#### Phase目标
- 将 I2S 接收结果通过 FIFO 与 APB 寄存器暴露给 CPU，形成可观测、可控制的数据入口。

#### 具体任务清单
- 设计 `audio_fifo`
- 设计 `audio_ctrl_apb`
- 完成 APB 扩展口挂接
- 增加寄存器：
  - 使能/停止
  - FIFO 水位
  - 样本读取
  - 中断状态/清零
  - 溢出标志
- 先实现“CPU 轮询读 FIFO”的最小模式
- 再补充“FIFO 达阈值中断”机制
- 编写 testbench 验证 APB 读写和 FIFO 状态转移

#### 涉及模块
- `audio_fifo.v`
- `audio_ctrl_apb.v`
- `apb_subsystem.v`
- `top.v`
- `tb_audio_ctrl_apb.v`

#### 验证策略（Testbench + 上板）
- Testbench：
  - APB 写使能、读状态、读数据
  - 检查 FIFO 计数和中断行为
  - 将假样本灌入 FIFO，验证 CPU 可见
- 上板：
  - 先做寄存器读写冒烟验证
  - 可用 GPIO/UART 打印寄存器值辅助检查

#### 预期交付物
- APB 录音控制外设 RTL
- FIFO RTL
- 寄存器说明文档
- 冒烟测试软件

#### 计划完成时间
- 第5天 - 第6天

#### 依赖关系
- 依赖阶段2输出 `sample_valid` 和 `sample_data`

#### 验收标准
- CPU 能通过 APB 读到连续样本
- FIFO 水位、空满、溢出状态正确
- APB 外设可稳定集成到当前 SoC

### 阶段4：存储通路最小闭环开发（必须）

#### Phase目标
- 不接真实音频，先把“样本流写入片外存储”的主链路打通。

#### 具体任务清单
- 选定并实现主线存储控制模块：
  - 推荐 `spi_flash_ctrl`
  - 备选 `sdram_write_ctrl`
- 暂不直接接 I2S，先用固定递增样本或固定模式样本作为输入源
- 实现写地址递增、写长度统计、停止条件
- 增加存储忙状态、错误状态、完成标志
- 若选 SPI FLASH：
  - 实现初始化、擦除策略、页写策略、忙等待
- 若选 SDRAM：
  - 实现初始化、突发写、地址管理
- 编写 testbench，验证写入地址和数据内容

#### 涉及模块
- `storage_ctrl.v`
- `spi_flash_ctrl.v` 或 `sdram_write_ctrl.v`
- 对应 memory model / mock model
- `tb_storage_ctrl.v`

#### 验证策略（Testbench + 上板）
- Testbench：
  - 输入固定 0x0001, 0x0002, 0x0003... 样本流
  - 检查存储内容、地址递增和结束条件
- 上板：
  - 仅验证最小假数据写入
  - 导出存储内容并与预期模式比对

#### 预期交付物
- 存储控制 RTL
- 存储接口 testbench
- 假数据写入验证记录
- 存储导出说明

#### 计划完成时间
- 第7天 - 第9天

#### 依赖关系
- 依赖阶段1主线存储方案冻结
- 可并行参考阶段3的 FIFO 输出接口

#### 验收标准
- 存储器中可读回连续正确的假数据
- 完成一次完整“生成数据 -> 写存储 -> 导出 -> 比对”的闭环

### 阶段5：录音基本功能闭环联调（必须）

#### Phase目标
- 将真实 I2S 音频接入存储通路，完成课程作业最低可验收版本。

#### 具体任务清单
- 将 `i2s_rx_core -> audio_fifo -> storage_ctrl` 串接
- 编写 CPU 控制程序：
  - 初始化录音外设
  - 配置录音长度
  - 启动/停止录音
  - 轮询或中断方式监控完成
- 上板连接 INMP441：
  - BCLK
  - LRCLK/WS
  - SD
  - 电源与地
- 录制 10s 音频并导出
- 再录制 30s 音频并导出
- 在 PC 端将原始 PCM 加 WAV 头，生成可播放文件

#### 涉及模块
- `top.v`
- `i2s_rx_core.v`
- `audio_fifo.v`
- `audio_ctrl_apb.v`
- `storage_ctrl.v`
- `cnasic_sleep/App/main.c`

#### 验证策略（Testbench + 上板）
- Testbench：
  - 使用 I2S 激励源模拟 1kHz 正弦或固定样本表
  - 检查写入存储的数据序列是否正确
- 上板：
  - 先录短音频 1s-2s 做冒烟验证
  - 再做 10s 与 30s 正式验证
  - 用导出文件播放确认无爆音、严重错速、全零或明显错位

#### 预期交付物
- 可运行录音版本 SoC
- 可运行 CPU 控制程序
- 10s/30s 导出音频文件
- 最低验收演示材料

#### 计划完成时间
- 第10天 - 第11天

#### 依赖关系
- 强依赖阶段2、阶段3、阶段4全部完成

#### 验收标准
- 真实麦克风音频可采集
- 数据可成功写入片外存储
- 导出后经 PC 打包为 WAV 可播放
- 满足课程作业基本要求

### 阶段6：稳定性优化与工程固化

#### Phase目标
- 解决联调阶段暴露的边界问题，使系统达到可重复演示和可提交状态。

#### 具体任务清单
- 检查并修复：
  - FIFO 溢出
  - 录音起始样本错位
  - 停止瞬间丢样
  - 存储地址越界
  - 时钟域边界问题
- 增加调试寄存器和统计计数器
- 增加异常处理：
  - 录音忙时禁止重复启动
  - 存储忙时告警
  - FIFO 溢出锁存
- 整理最终版地址文档、时序说明、导出流程

#### 涉及模块
- 全部录音相关 RTL
- `main.c`
- 调试文档

#### 验证策略（Testbench + 上板）
- Testbench：
  - 长时间连续样本输入
  - 边界停止、复位重启、异常重复启动
- 上板：
  - 连续多次录音回归测试
  - 不同录音时长测试

#### 预期交付物
- 稳定版 RTL
- 稳定版软件
- 回归测试记录
- 最终接口文档

#### 计划完成时间
- 第12天

#### 依赖关系
- 依赖阶段5形成完整闭环

#### 验收标准
- 连续多次录音结果一致
- 不再出现随机失败、卡死或明显异常音频

### 阶段7：WAV打包软件化（进阶）

#### Phase目标
- 由 CPU 在 SoC 内完成 WAV 头打包，减少 PC 侧后处理工作。

#### 具体任务清单
- 设计 WAV 文件头写入流程
- 在录音开始前预留文件头区域
- 录音结束后回填文件长度、采样率、字节率、数据块长度
- 评估对存储介质“随机改写”能力的要求
- 更新导出流程，使 PC 端直接得到 `.wav`

#### 涉及模块
- `main.c`
- 存储访问驱动

#### 验证策略（Testbench + 上板）
- Testbench：
  - 重点验证长度字段计算逻辑
- 上板：
  - 直接导出 `.wav`
  - PotPlayer 直接播放

#### 预期交付物
- CPU WAV 打包代码
- WAV 头格式说明
- 直接可播放的导出文件

#### 计划完成时间
- 第13天

#### 依赖关系
- 依赖阶段5闭环已完成

#### 验收标准
- 导出文件无需 PC 侧补头即可直接播放

### 阶段8：IMA-ADPCM硬件编码（进阶）

#### Phase目标
- 在不影响基本要求交付的前提下，完成 IMA-ADPCM 压缩原型验证。

#### 具体任务清单
- 设计 `ima_adpcm_encoder`
- 明确编码输入输出接口
- 插入位置建议为 `audio_fifo` 后、`storage_ctrl` 前
- 先做模块级编码正确性验证
- 再做 SoC 集成开关，支持 PCM/ADPCM 两种模式
- 准备 PC 端解码或播放器兼容验证方案

#### 涉及模块
- `ima_adpcm_encoder.v`
- `tb_ima_adpcm_encoder.v`
- `audio_ctrl_apb.v`
- `main.c`

#### 验证策略（Testbench + 上板）
- Testbench：
  - 与软件参考模型逐帧比对
- 上板：
  - 录音导出
  - PC 端解码回 WAV 后播放

#### 预期交付物
- IMA-ADPCM RTL
- 编码 testbench
- 编解码验证记录

#### 计划完成时间
- 第14天 - 第15天

#### 依赖关系
- 严格依赖阶段5完成，且不得反向破坏基本功能

#### 验收标准
- 编码数据可被正确解码还原
- 录音主线功能保持稳定

## 5. 15天总体排期总览

| 天数 | 阶段 | 关键产出 |
| --- | --- | --- |
| Day 1-2 | 阶段1 | 方案冻结、地址表、寄存器表、接口文档 |
| Day 3-4 | 阶段2 | I2S 接收 RTL + testbench |
| Day 5-6 | 阶段3 | APB 控制外设 + FIFO + CPU 可读样本 |
| Day 7-9 | 阶段4 | 存储控制闭环 + 假数据写存储验证 |
| Day 10-11 | 阶段5 | 真实录音闭环，满足基本要求 |
| Day 12 | 阶段6 | 稳定性修复与回归 |
| Day 13 | 阶段7 | CPU 侧 WAV 打包 |
| Day 14-15 | 阶段8 | IMA-ADPCM 原型验证 |

## 6. 建议分工方式

### 6.1 双人分工
- 方向A：RTL主线
  - I2S 接收
  - FIFO
  - APB 外设
  - 存储控制
  - testbench
- 方向B：软件与验证主线
  - Keil 驱动与控制程序
  - 数据导出脚本
  - WAV 打包
  - 上板连线与联调记录

### 6.2 三人分工
- 成员1：I2S + FIFO + APB 外设
- 成员2：存储控制 + Quartus 上板
- 成员3：CPU 软件 + 导出工具 + 验证材料整理

### 6.3 分工协作约束
- 任一模块负责人必须同时负责该模块 testbench
- RTL 与软件接口一旦冻结，不得随意修改寄存器语义
- 每晚合入一次可编译版本，避免分支长期漂移

## 7. 关键风险与应对

### 7.1 I2S时序理解错误
- 风险：采样边沿或 LRCLK 对齐错误导致整体音频失真
- 应对：先做模块级理想激励仿真，再接真实麦克风

### 7.2 存储控制复杂度超预期
- 风险：SPI FLASH 页写或 SDRAM 初始化调不通，拖慢主线
- 应对：第2天前冻结主线方案；优先选择实现复杂度更低的路径

### 7.3 上板调试时间被压缩
- 风险：RTL 仿真通过，但引脚、时钟、复位或约束问题影响上板
- 应对：第10天前必须完成主线仿真闭环，保留至少 3 天上板和修复时间

### 7.4 数据导出流程不顺畅
- 风险：虽然录到数据，但无法快速转成播放器可识别格式
- 应对：基本要求阶段允许 PC 侧补 WAV 头；CPU 侧打包放到进阶阶段

### 7.5 ADPCM影响主线
- 风险：进阶编码插入过早，破坏基本链路稳定性
- 应对：ADPCM 仅在基本要求验收后开始，不得阻塞主线提交

## 8. 最终验收标准

### 8.1 基本要求验收
- I2S 成功采集 INMP441 左声道音频
- 音频数据成功写入 SDRAM 或 SPI FLASH
- 可从存储介质导出录音数据
- 导出后可生成 `.wav` 并使用 PotPlayer 正常播放
- 至少完成一次 10s 录音和一次 30s 录音验证

### 8.2 进阶要求验收
- CPU 可直接输出 WAV 格式文件
- IMA-ADPCM 编码结果可正确解码与播放

### 8.3 文档与工程验收
- RTL、testbench、Keil 工程可交付
- 地址映射、寄存器表、使用说明完整
- 关键仿真截图与上板验证记录完整

## 9. 里程碑结论
- 第6天前，必须看到“CPU 可读到样本”的可见结果。
- 第9天前，必须看到“假数据写入片外存储”的闭环结果。
- 第11天前，必须完成课程作业基本要求。
- 第12天以后，所有工作以“稳定性”和“进阶功能”优先，不再推翻主线架构。
